<Window x:Class="Bitness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="800" Width="1280" 
        Loaded="MainWindow_Loaded"
        Closing="MainWindow_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
        <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
        <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    </Window.Resources>
    <Grid>
        <Border Grid.Row="1" Background="Black" />
        <Border Grid.Row="1" Grid.Column="2" Background="Black" Margin="0,0,8,0"/>
        <Border Grid.Column="0" Background="Black"/>
        <Border Grid.Column="2" Background="Black" Margin="0,0,8,0"/>
        <Border Grid.Column="1" Background="Black"/>
        
        <Viewbox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Grid Name="CompositeImage">
                <Image Source="{Binding ImageSource}" Stretch="UniformToFill" />
                <Image Source="{Binding BodySource}" Stretch="UniformToFill" />
            </Grid>
        </Viewbox>
        
        <StatusBar HorizontalAlignment="Stretch" Name="statusBar" VerticalAlignment="Bottom" Background="White" Foreground="{StaticResource MediumGreyBrush}">
            <StatusBarItem Content="{Binding StatusText}" />
        </StatusBar>
        
        <Image Grid.Row="0" Source="http://bitnessfitness.github.io/images/team.png" HorizontalAlignment="Center" Stretch="Fill" Height="56" Width="95" Margin="0 10 0 5" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="0 0 -1 0" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource KinectBlueBrush}" FontFamily="Segoe UI" FontSize="36">BITNESS</TextBlock>
        <Image Grid.Row="0" Stretch="None" HorizontalAlignment="Center" Margin="0 0 0 5" />
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="880"/>
            <ColumnDefinition Width="200"/>
            <!--
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
